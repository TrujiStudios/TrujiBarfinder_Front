import { styled } from '@mui/system';

const useStyles = styled('div')({
    swalConfirm: {
        marginRight: '10px',
    },
    // Aquí puedes agregar más estilos si los necesitas
});

export default useStyles;